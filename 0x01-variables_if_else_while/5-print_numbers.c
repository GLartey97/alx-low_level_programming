#include <stdio.h>
/**
 * main - all base 10 digits
 * initialize i
 * Return: Always 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	putchar('\n');
	return (0);
}
