#include "main.h"
/**
 * print_last_digit - prints the last digit
 * @n: number
 * Return: value of last digit
 */
int print_last_digit(int n)
{
	int ld = n % 10;

	if (ls < 0)
		ld *= -1;

	_putchar(ls + '0');

	return (0);
}
